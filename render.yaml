services:
  - type: web
    name: ai-budget-prediction
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: FLASK_DEBUG
        value: false
      - key: PYTHON_VERSION
        value: 3.11.11
    disk:
      name: data
      mountPath: /data
      sizeGB: 1
